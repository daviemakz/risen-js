'use strict';

// Define the log path
export const LOG_PATH = './tmp/dev/runtime.log';

// Define SSL variables
export const SSL_OPTIONS = {
  key: './__resources__/ssl/server.key',
  cert: './__resources__/ssl/server.crt'
};
